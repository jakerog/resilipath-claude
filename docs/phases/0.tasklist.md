# Phase 0 Tasklist — Foundation & Infrastructure
**Version:** 0.1.0-DRAFT  
**Date:** 2026-03-29  

---

## EPIC 0.1: Cloud Infrastructure
- [ ] 0.1.1 — Select cloud provider (decision gate: requires stakeholder input on existing cloud contracts)
- [ ] 0.1.2 — Design network topology (VPC, subnets, NAT, security groups)
- [ ] 0.1.3 — Provision dev environment via IaC (Terraform or Pulumi)
- [ ] 0.1.4 — Provision staging environment via IaC
- [ ] 0.1.5 — Provision production environment via IaC (locked; deploy-only via CI/CD)
- [ ] 0.1.6 — Configure auto-scaling policies (application tier)
- [ ] 0.1.7 — Configure managed database service (primary + read replica)
- [ ] 0.1.8 — Configure Redis cluster (cache + session + pub/sub)
- [ ] 0.1.9 — Configure object storage (S3-compatible, per-tenant bucket policy)
- [ ] 0.1.10 — Configure CDN for static assets and file delivery
- [ ] 0.1.11 — Configure DNS and certificate management (wildcard cert for tenant subdomains)

## EPIC 0.2: Multi-Tenancy Architecture
- [ ] 0.2.1 — Architecture spike: evaluate row-level, schema-level, and DB-per-tenant isolation models
- [ ] 0.2.2 — Document chosen isolation model as ADR-001
- [ ] 0.2.3 — Implement `tenants` table and tenant provisioning service
- [ ] 0.2.4 — Implement ORM middleware that enforces tenant_id scoping on all queries
- [ ] 0.2.5 — Write integration tests: tenant A data cannot be accessed by tenant B (automated)
- [ ] 0.2.6 — Implement tenant onboarding API (create tenant, set plan, create initial admin user)
- [ ] 0.2.7 — Implement tenant subdomain routing (e.g., `acmecorp.resilienceos.com`)

## EPIC 0.3: Authentication & Authorization
- [ ] 0.3.1 — Select and configure auth provider (Auth0, Cognito, or self-hosted)
- [ ] 0.3.2 — Implement OIDC/OAuth 2.0 login flow (email/password + social)
- [ ] 0.3.3 — Implement MFA (TOTP-based) — required for Admin and Moderator roles
- [ ] 0.3.4 — Implement SAML 2.0 SSO stub (full implementation in Phase 2)
- [ ] 0.3.5 — Design and implement RBAC framework (roles, permissions, policy enforcement)
- [ ] 0.3.6 — Implement JWT access token + refresh token pattern
- [ ] 0.3.7 — Implement session management (idle timeout, active exercise extension)
- [ ] 0.3.8 — Write RBAC unit tests for all defined role/permission combinations

## EPIC 0.4: API Framework
- [ ] 0.4.1 — Scaffold API server (language/framework per ADR)
- [ ] 0.4.2 — Implement OpenAPI 3.1 spec-first workflow (generate routes from spec)
- [ ] 0.4.3 — Implement API versioning strategy (`/api/v1/...`)
- [ ] 0.4.4 — Implement rate limiting middleware (per-endpoint, per-tenant, per-user)
- [ ] 0.4.5 — Implement standardized error response format
- [ ] 0.4.6 — Implement request logging and correlation ID propagation
- [ ] 0.4.7 — Implement health check endpoint (`/health`, `/ready`)
- [ ] 0.4.8 — Implement cursor-based pagination utility

## EPIC 0.5: Audit Log System
- [ ] 0.5.1 — Design audit log schema (tenant_id, user_id, action, entity_type, entity_id, before_state, after_state, timestamp, ip_address)
- [ ] 0.5.2 — Implement audit log write service (append-only, dedicated schema)
- [ ] 0.5.3 — Implement audit log middleware that captures all mutating API calls automatically
- [ ] 0.5.4 — Write tests: audit log cannot be updated or deleted

## EPIC 0.6: CI/CD Pipeline
- [ ] 0.6.1 — Configure repository (monorepo structure with Turborepo or Nx)
- [ ] 0.6.2 — Implement lint stage (ESLint/Prettier or language equivalent)
- [ ] 0.6.3 — Implement unit test stage
- [ ] 0.6.4 — Implement integration test stage (against test DB)
- [ ] 0.6.5 — Implement SAST scan (CodeQL or Semgrep)
- [ ] 0.6.6 — Implement dependency audit (npm audit / Snyk)
- [ ] 0.6.7 — Implement build and container image build
- [ ] 0.6.8 — Implement staging deploy (auto on merge to main)
- [ ] 0.6.9 — Implement production deploy (manual approval gate)
- [ ] 0.6.10 — Implement smoke test suite (post-deploy)
- [ ] 0.6.11 — Configure branch protection rules (no direct push to main; PRs require review + CI pass)

## EPIC 0.7: Secrets Management
- [ ] 0.7.1 — Provision secrets manager
- [ ] 0.7.2 — Migrate all environment secrets out of `.env` files into secrets manager
- [ ] 0.7.3 — Configure secret rotation policy for database credentials
- [ ] 0.7.4 — Document secret access patterns for all services

## EPIC 0.8: Observability
- [ ] 0.8.1 — Select and configure observability platform
- [ ] 0.8.2 — Implement structured logging (JSON format, log levels, correlation IDs)
- [ ] 0.8.3 — Implement application performance metrics (request rate, latency, error rate)
- [ ] 0.8.4 — Implement infrastructure metrics (CPU, memory, DB connections, Redis memory)
- [ ] 0.8.5 — Configure alerting (P0: error rate spike, P0: DB connection pool exhaustion, P1: latency > 1s p95)
- [ ] 0.8.6 — Create engineering runbook for common alert responses
- [ ] 0.8.7 — Configure uptime monitoring (external, checks every 60 seconds)

## EPIC 0.9: Design System
- [ ] 0.9.1 — Define color token system (primary, secondary, semantic, neutral, status colors)
- [ ] 0.9.2 — Define typography scale (font family, sizes, weights, line heights)
- [ ] 0.9.3 — Define spacing system (4px base unit)
- [ ] 0.9.4 — Define shadow/depth system (Skeuomorphic depth levels)
- [ ] 0.9.5 — Build base components: Button, Input, Select, Checkbox, Toggle, Badge, Card, Modal, Toast
- [ ] 0.9.6 — Build navigation components: Sidebar, TopBar, TabBar (mobile)
- [ ] 0.9.7 — Configure Storybook (component documentation and visual testing)
- [ ] 0.9.8 — Define PWA manifest and icons (multiple sizes, splash screens)

## EPIC 0.10: Tenant Onboarding Flow
- [ ] 0.10.1 — Sign-up page (email, password, company name, plan selection)
- [ ] 0.10.2 — Email verification flow
- [ ] 0.10.3 — Workspace creation (subdomain assignment, default admin user)
- [ ] 0.10.4 — Initial setup wizard (organization name, logo upload, timezone, invite team members)
- [ ] 0.10.5 — Subscription management integration (Stripe or equivalent — stub for MVP)
